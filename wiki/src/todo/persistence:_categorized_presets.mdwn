At t-p-s configure step, sort items by category: personal data, app
configuration, advanced; custom lines would go to advanced; advanced
would be displayed if, and only if, at least one custom line is
already in live.persist; else, an arrow icon allows to
unroll/display it.

[[!tag todo/code]]
