This ticket tracks the progress of replacing certain steps of the
[[manual test suite|contribute/release_process/test]] with automated
tests in `features/checks.feature`.

# Tests that needs a review:

The following automated tests need a review from someone else than
anonym after which they can be retagged `todo/test_pass`:

[[!tag todo/qa]]

* `modinfo vboxguest` should work when booted on the 686-pae kernel

# Related tickets

* [[todo/test_suite:_shipped_signing_key_scenario]]
* [[todo/test_suite:_mat_scenario_fails]]
