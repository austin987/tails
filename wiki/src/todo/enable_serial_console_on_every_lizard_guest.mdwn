[[!tag todo/sysadmin]]

Enable serial console in every guest, to be able to connect from the
host without setting up SSH authentication. While we're at it, enable
it for Grub and on the kernel cmdline + inittab to ease
emergency maintenance.
