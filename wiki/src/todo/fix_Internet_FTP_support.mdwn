In Tails 0.12.1, I manage to connect to a FTP server on the
Internet (such as `ftp.de.debian.org`) neither with iceweasel nor with
GNOME "Connect to server" feature.

At least the latter works properly to connect to a FTP server on the
LAN by IP.

[[!tag todo/code]]


[[!tag broken_window]]


[[!tag release/1.0]]
