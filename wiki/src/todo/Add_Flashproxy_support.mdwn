We need to start porting
[Flashproxy](https://crypto.stanford.edu/flashproxy/) to Tails, and
add the option to use Flashproxy in
[[doc/first_steps/startup_options/bridge_mode]] at boot. If we do
this, we also need to document it (as always).

[[wishlist]]
