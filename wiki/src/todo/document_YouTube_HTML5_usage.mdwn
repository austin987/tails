NoScript blocks YouTube HTML5 by default and users are confused.
We need to document how to bypass this protection if they wish.

> We dropped the click-to-play barrier, so that's now a non-issue.

[[!tag todo/done]]
