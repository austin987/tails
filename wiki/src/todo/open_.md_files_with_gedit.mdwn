There's no default application selected for opening `.md` files.

I propose to open those files with gedit

> At first sight, it looks like this suggestion is sound.
>
> However, this should be implemented upstream, so I suggest you first
> check how Nautilus behaves in Debian Wheezy, and report bugs against
> Debian if it does not behave the way you want.

[[wishlist]]
