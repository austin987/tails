When we merged `feature/automated_tests/cucumber` into `stable`, we've
reverted commit `255aa71` ("Make sure the memory dumping debug message
is shown cleanly") to not mess anything up for the imminent 0.17.2
release at the time. However, the `erase_memory` feature depends on
it, so we now must consider reverting that revert.

> Done in devel and experimental, we should now test (using one of
> our old non-automated ways) if this gives the same results:
>
> * on a system with PAE
> * on a system without PAE

[[!taglink todo/qa]]
