test Windows autorun (should display the included static build of this
very wiki)

> This does currently not work: line terminators must be CR+LF in
> autorun.inf and autorun.bat; Windows do not understand the
> content otherwise.

>> Converted line terminators in this very commit.
>> Please re-test and tag as done if it works.

[[!tag todo/test]]

If it does not work, see guadalinex's
[autorun configuration](http://forja.guadalinex.org/guadalinexv6/gensys/live-helper/guadalinexv6/config/binary_local-includes/).
