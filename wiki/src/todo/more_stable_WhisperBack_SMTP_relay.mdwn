WhisperBack SMTP relay is too often down.

We should setup another instance of it on a more stable server
(waiting our new server to be back online).

Having two boxes providing this (hidden) service would bring us
cheap failover.

[[!tag todo/code]]
