[[!tag todo/wait]]

The Debian X strike force will maintain Xorg backports for Squeeze and
has started uploaded a set of packages already.

As soon as they are complete, we probably want to test and use these.
(Probably with an updated matching kernel, which will anyway be needed
for [[better SquashFS compression|todo/use_lzma_compression]].)
