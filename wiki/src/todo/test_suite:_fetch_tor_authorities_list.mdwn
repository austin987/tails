In `features/support/config.rb` we have a static list of the IP
addresses of the Tor authorities used by the firewall leak
detector. It'd be great if we could fetch it dynamically, but with
up-to-date info, and with authentication.

[[!tag todo/research]]
