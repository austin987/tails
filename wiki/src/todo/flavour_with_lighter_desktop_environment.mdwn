[[!tag todo/discuss]]

- replace XFCE with LXDE?
- test XFCE images and update XFCE config
- cleanup `.config/xfce*` if XFCE is chosen

256MB is currently enough to boot the system and use iceweasel +
Pidgin => wontfix => [[!tag todo/done]].

