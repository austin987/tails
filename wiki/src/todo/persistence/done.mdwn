# live-boot

* `persistent-read-only` boot parameter
* Changing `persistent-encryption` to accept a list of types
* Making persistent-read-only work for live-rw and home-rw overlays
* `persistent-media=removable` or `persistent-media=removable-usb` (in
  `live-boot` 3.x).
* Refactoring overlay and snapshot scanning code
* Fully rework arguments: `persistent-method=`, `persistent-storage=`,
  `persistent-encryption=luks`, etc.
