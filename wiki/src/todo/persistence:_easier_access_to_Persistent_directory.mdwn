It would be nice to have a shortcut to the /persistent folder in
Places → Persistent

This should be configurable in `~/.gtk-bookmarks`.

Of course, this should check if the persistence is activated for the "Persistent" folder.

> Implemented in `feature/persistent-directory-in-places`,
> [[!taglink todo/done]] in 0.16.

[[!tag release/2.0]]
