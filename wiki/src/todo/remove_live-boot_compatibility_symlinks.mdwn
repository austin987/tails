Now that we have a [[todo/newer_live-boot]], in 0.19 or later, remove
our `/live` and `/live/image` compatibility symlinks, hope nothing
breaks, see what breaks, fix.

Implemented in `feature/remove_live-boot_compatibility_symlinks`.

[[!tag pending]] for for 0.19.
