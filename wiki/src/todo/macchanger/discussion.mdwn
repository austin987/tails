Typo: "so we need to device a simple" should be "so we need to devise a simple". I'd have fixed it myself but apparently something called blogspam thinks this page has too many links, so it can't be edited.
