We [[totally blocked IPv6 to avoid some leaks|todo/really_block_ipv6]]
we erroneously thought (see commit a3ca665f) our firewall let through.

On the long run, we want IPv6 to work to some extent (e.g.
for [[OnionCat|todo/install_OnionCat]]). So, let's re-enable IPv6.

[[!taglink todo/done]] in 0.18.
