We [[totally blocked IPv6 to avoid some leaks|todo/really_block_ipv6]]
our firewall let through for some reason.

But on the long run, we want IPv6 to work to some extent (e.g.
for [[OnionCat|todo/install_OnionCat]]), so the root cause should be
found.

Let's [[!taglink todo/research]] the root cause of the issue.
