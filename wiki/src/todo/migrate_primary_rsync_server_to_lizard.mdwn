[[!tag todo/sysadmin]]

Done:

1. write `tails::rsync`
1. deploy `tails::rsync`
1. import data
1. forward a port to the VM's rsyncd
1. test
1. switch rsync.t.b.o DNS to rsync.lizard

Left to do:

1. have the public rsync server pull directly from rsync.t.b.o
1. update mirrors sync doc
1. ask b.o to drop the rsync setup they hosted for us
