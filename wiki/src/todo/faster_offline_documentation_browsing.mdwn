Tails's offline documentation via Yelp is slow. The problem is related
to the '*box-shadow' CSS properties that are rendered very slowly by
the version in Squeeze.

Fix in `bugfix/fix_slow_offline_documentation`.

[[!tag todo/qa]]

[[!tag release/1.0]]
