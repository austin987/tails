The Windows look would be the auto-selected default in case a Windows
OS is detected on fixed disks": can be implemented right now (probably
using os-prober, /etc/grub.d/30_os-prober may be worth a look).
This should only be done when we've agreed that the theme is in a good
enough state for end-users.

[[!tag todo/code]]

