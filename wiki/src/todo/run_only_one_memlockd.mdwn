Apparently, two instances of `memlockd` are running.

May it be a race condition at boot time, when
`tails-reconfigure-memlockd` restarts it?

Let's [[!taglink todo/test]] if reloading `memlockd` there, instead of
restarting it, fixes the issue.
