The default screensaver waiting time in Tails is set to 30 minutes,
nonetheless the screen turns black after 10 minutes at most.

Running `xset -dpms s off` fixes this behavior. We should fix that in
the ISO somewhere...

[[!tag todo/code]]
