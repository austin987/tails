When persistence is activted in RO mode, aufs mounts are
shown as devices in nautilus's device list. This is confusing.

I found this issue in 0.17.1

> This can probably be fixed using a similar trick as suggested on
> [[todo/hide_only_current_running_TailsData]].

[[!tag todo/code]]
