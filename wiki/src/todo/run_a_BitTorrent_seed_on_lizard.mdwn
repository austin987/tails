[[!tag todo/done]]

It would be nice to have another BitTorrent seed setup on lizard,
especially in case a.t.b.o is decommissioned.

If there's an Augeas lens to edit
`/etc/transmission-daemon/settings.json`, use it.
Else, deploy the full configuration file with Puppet.
