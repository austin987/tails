See [[doc/encryption_and_privacy/virtual_keyboard]].

- Mention both software and hardware keyloggers.
- Warn that password created with Tails greeter can be captured by a *hardware* keylogger. (and give instructions for workaround: Create *temporary*, insecure password then change immediately after Tails has fully using virtual keyboard and `passwd` command.

- Add link on documentation about keyloggers.
- Include Florence screenshot and desktop icon.
- Rename the page to mention explicitly the virutal keyboard.

[[!tag todo/documentation]]
