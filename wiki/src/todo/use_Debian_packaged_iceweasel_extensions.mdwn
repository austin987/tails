It is really easier to maintain Debian-packaged iceweasel extensions,
rather than upgrading them in the Git-ified `$HOME`.

CS Lite
=======

[[!taglink todo/research]] if another nice cookie manager is already
packaged. If not, package this one or similar.

Adblock Plus
============

Now available in Squeeze, but depends on newer iceweasel (>= 3.0.9).
Can it be backported and this versionned dependency removed? [[!tag todo/research]]
