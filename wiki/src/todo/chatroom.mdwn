As any user can change the password for the shared Jabber account, and
it globally seems that XMPP causes more harm than good in our case,
let's use the #tails IRC chan on oftc.net with a randomized nick.

This very commit implements this => [[!taglink pending]].
