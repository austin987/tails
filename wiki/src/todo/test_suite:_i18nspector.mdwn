Run i18nspector during the automated test suite to catch errors in the PO
files for our custom scripts and website.

  - PO files are to be found in
    `config/chroot_local-includes/usr/share/locale/` and `wiki/src/`
  - our `check_po.sh` wrapper or i18nspector should be run

[[!tag todo/code]]
