We could add a "About Tails" launcher in the "System" menu,
just bellow (or instead of) the "About GNOME" one.
It would report roughly the same information as `tails-version`.

> The only way to add a new launcher to the "System" menu
> is to patch panel-menu-items.c in gnome-panel source code.
>
> It is however easy to replace "About GNOME" by anything else
> by modifying gnome-about.desktop.
>
> This is done in the branch `feature/about_tails`

[[!taglink pending]] for 0.18.

