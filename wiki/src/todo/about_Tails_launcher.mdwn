We could add a "About Tails" launcher in the "System" menu,
just bellow (or instead of) the "About GNOME" one.
It would report roughly the same information as `tails-version`.

[[!tag todo/code todo/easy]]
