Installing a bunch of firmwares is not enough to properly support most
wifi cards. See `examples/hooks/{ipw3945,madwifi,rt}*.sh` in the
`live-helper` repository for additional wifi modules support.

Make sure to remove `build-essential`, `module-assistant` and their
dependencies after using them.
