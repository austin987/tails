[[!tag todo/code]]

Installing a bunch of firmwares is not enough to properly support most
wifi cards.

We could take a look to the Debian Backtrack work around wifi and patches for injection.

> We now build rt2570 modules, untested yet => [[!taglink todo/test]].
> Once it works properly, it can serve as an example to build other
> modules that also use `module-assistant`.
