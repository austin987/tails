[[!tag todo/code]]

Installing a bunch of firmwares is not enough to properly support most
wifi cards.

We could take a look to the Debian Backtrack work around wifi and patches for injection.

> We now build the broadcom-sta, rt2570 and rt2860 modules.
> Untested yet => [[!taglink todo/test]].
>
> Once our generic `51-module-assistant` hook works properly, it can
> serve as an example to build other modules that also use
> `module-assistant` if needed.
