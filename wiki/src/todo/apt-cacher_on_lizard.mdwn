[[!tag todo/done]]

The shared SeaCCP APT cache (macaw) is quite overloaded and slow at
times, and we're going to use it intensively, so we should setup our
own cache (and either use macaw as a backend, or avoid all kinds of
double-cache problems by connecting directly to a Debian mirror).

