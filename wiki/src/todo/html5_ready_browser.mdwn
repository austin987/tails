Could a more recent browser with support for html5 be included in the image? There are plenty of html5 supporting browsers that are open source, and it doesn't have to be the default browser, but html5 replaces a lot of what flash does, without the cost to privacy that flash carries.
