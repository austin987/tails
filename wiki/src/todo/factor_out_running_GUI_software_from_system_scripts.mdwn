We need a uniform way to start GUI applications from system scripts:
  - `/etc/NetworkManager/dispatcher.d/60-iceweasel.sh`
  - `/usr/local/sbin/restart-vidalia`

Thus should be factored out in the Tails shell library.

[[!tag todo/code]]
[[!parent factor_out_stuff_into_a_shell_library]]
