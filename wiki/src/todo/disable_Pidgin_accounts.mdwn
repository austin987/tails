1. explain how to enable them in: [[!tag todo/documentation]]
   1. Help & Support → Get in touch with us → Chat
   2. Documentation → Connect to the Internet anonymously → Chatting
      with Pidgin & OTR
1. Apply the following patch to disable the only Pidgin account
   that's currently enabled:

	diff --git a/config/chroot_local-includes/etc/skel/.purple/accounts.xml b/config/chroot_local-includes/etc/skel/.purple/accounts.xml
	index 38af191..5b1c319 100644
	--- a/config/chroot_local-includes/etc/skel/.purple/accounts.xml
	+++ b/config/chroot_local-includes/etc/skel/.purple/accounts.xml
	@@ -23,7 +23,7 @@
	                        <setting name='ssl' type='bool'>1</setting>
	                </settings>
	                <settings ui='gtk-gaim'>
	-                       <setting name='auto-login' type='bool'>1</setting>
	+                       <setting name='auto-login' type='bool'>0</setting>
	                </settings>
	                <current_error/>
	        </account>
