laptop-mode-tools has been shipped for a while in Tails.

Using its `wireless*power` modules would probably work on more
hardware, in a more robust way, than the current custom `tails-wifi`
initscript.

[[!tag todo/code todo/easy]]
