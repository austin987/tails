Vidalia 0.2.12 supports Tor's ControlSocket as an alternative to
ControlPort.

This can make Tails source a bit simpler: no need to set a custom
`CookieAuthFile`.

We now need to [[!taglink todo/wait]] for Tor 0.2.2 to turn into a
stable release so that we can make this change.
