Display the same login screen (with persistence option) regardless of whether a persistent volume is present, so that without the passphrase an adverse party would not know whether a persistent volume is present.

> It's trivial, for an adversary who has access to a Tails USB stick,
> to tell whether it contains a persistent volume: it has a LUKS
> header. So, the suggested change would not bring much security,
> and I'm therefore closing this ticket. [[!tag todo/done]] Feel free to
> reopen it with more information if you deem it useful :)
>
> Also see [[todo/create_encrypted_TailsData_by_default]], that should
> be a better solution to the problem I guess you wanted to see fixed.
