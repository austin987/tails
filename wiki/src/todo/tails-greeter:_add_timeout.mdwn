To allow unattended booting, we could add a timer (maybe 10 s) to
TailsGreeter which defaults to normal (no persistence, no advanced
options) Tails boot unless some kind of activity is noticed.

[[!tag todo/code]]
