The VirtualBox guest tools / modules backport shipped in Tails is
getting old. One should upgrade it to match current Wheezy's one.

Note that the backport must be build in a pbuilder chroot *that has,
or prefers, the XOrg backported stack*.

[[!tag todo/code]]
