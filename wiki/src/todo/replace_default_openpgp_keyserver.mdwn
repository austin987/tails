The default keyserver used by GPG in Tails has been unreliable if not
unreachable for some time. We replace it with a more reliable one,
such as the hkps pool.

Implemented in `bugfix/use-more-reliable-keyserver`.

> [[!taglink todo/done]] in 0.17.2.
