At t-p-s configure step, window doesn't fit vertically on a 1024x600
display (the "Save" button is hidden bellow the bottom GNOME taskbar)

Perhaps maximize the window vertically in all cases?

[[!tag todo/code]]
