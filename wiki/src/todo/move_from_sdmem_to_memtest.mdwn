Rationale
=========

Replace sdmem with the Linux kernel's `memtest=2` feature will fix
[[bugs/sdmem_does_not_clear_all_memory]] and will generally be simpler
and more robust.

Plans
=====

1. [[!taglink todo/wait]] for a Debian kernel (will be
   enabled in the next (> 3.1.4-1) one) to ship with memtest
   option enabled.
2. imitate Liberté Linux and use the Linux kernel's `memtest=2`
   feature
