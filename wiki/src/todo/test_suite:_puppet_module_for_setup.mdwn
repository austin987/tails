The [[contribute/release_process/test/setup]] documentation should be
turned into a Puppet module, so that we can deploy this on lizard (and
so that individual developers can reproduce this setup in a way that's
easy to keep up-to-date with future changes).

[[!tag todo/sysadmin]]
