   <p class="mac">Immediately press-and-hold the
   <span class="keycap">Option</span> key until a list of possible startup
   disks appears.</p>

   <p class="mac">Choose the USB stick and press
   <span class="keycap">Enter</span>. The USB stick appears as an external hard disk and might be labelled <span class="guilabel">EFI</span> or <span class="guilabel">Windows</span> like in the following screenshot:</p>

   [[!img inc/screenshots/mac_startup_usb.png class="screenshot mac" link="no" alt="Screen with the logo of an internal hard disk labelled 'Macintosh HD' and an external hard disk labelled 'Windows' (selected)"]]
