[[!meta title="Git merge policy: review process"]]

Checks

- check the diff e.g. with `git log -p`
- build the branch (or experimental) and test the feature
- check the documentation
- check the ticket

Merge the branch with `--no-ff` :

- for a new feature : in `devel`
- for a fix on top on the rc: in `testing` *and* `devel`

Tag the ticket `pending`, reply to the email alking for review and push your changes.
