# FIXME: this sudo rule should be replaced by a polkit rule once we have
# policykit >= 0.106. The rule is already in:
# /etc/polkit-1/rules.d/org.boum.tails.additional-software.rules
amnesia ALL = NOPASSWD: /bin/systemctl start tails-additional-software-install.service
